[workspace]
members = [
    "chsc",
    "chs_lexer",
    "chs_ast",
    "chs_types",
    "chs_util",
    "chs_codegen", "test_maker",
]
resolver = "2"

package.version = "0.1.0"

[profile.release]
panic = "abort"

[profile.release-debug]
inherits = "release"
debug = true
